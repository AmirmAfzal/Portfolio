import React from "react";

interface Props {}

const SkillCard = (props: Props) => {
  return <div className="flex flex-row bg-base-200">
    <span className="text-lg">Next.js</span>
  </div>;
};

export default SkillCard;
